import { View, Text } from 'react-native';

const Chat = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Chat Page</Text>
    </View>
  );
};

export default Chat;
